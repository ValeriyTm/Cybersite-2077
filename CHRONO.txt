-Создаю файловую структуру проекта.
-В корне проекта делаю "npm init -y".
-В корневой package.json добавляю:
"workspaces": ["apps/_", "packages/*"]
-

<!--Занимаюсь структурой Frontend:-->

*В папке apps выполняю команду:
npm create vite@latest web -- --template react-ts
-!Не выполняю установку папки node_modules в apps/web
*Папке apps/web выполняю команду:
rm -rf .git
*Запускаю фронтенд из корня всего проекта (а не из фронтендовской папки) командой:
npm run dev -w web
*Меняю стандартную струтуру папки src на FSD:
-Создаю в src папки app, pages, widgets, features, entities, shared. Main.tsx оставляю в корне папки src. Глобальные стили помещаю в app/styles. Файлы из src/assets перемещаю в src/shared/assets. App.tsx перемещаю в src/app. Компоненты создаю в src/shared/ui/имя_компонента.
-Добавляю алиас. Сначала в конфиге vite добавляю импорт "import { fileURLToPath, URL } from "node:url";", а затем после plugins вставляю "resolve: {
      alias: {
        "@": fileURLToPath(new URL("./src", import.meta.url)),
      },
    },". После иду в конфиг tsconfig.app.json и добавляю в compilerOptions следующее: " "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }"
После этого могу начать использовать алиасы - теперь в путях символ "@" - это путь от src.
-Устанавливаю sass из корня проекта:
npm i -D sass -w web
Больше для работы sass делать не нужно ничего.
-Устанавливаю PostCSS и его плагины командой:
npm i -D postcss postcss-preset-env postcss-pxtorem -w web
Далее создаю в apps/web конфиг postcss.config.js и заполняю его.
Создаю в apps/web файл vite-env.d.ts, который будет говорить тайпскрипту не ругаться на импорты стилей.
-Устанавливаю StyleLint. Выполняю команду из корня:
npm install -D stylelint stylelint-config-standard-scss stylelint-config-clean-order -w web
Что есть что: stylelint-config-standard-scss - это стандартные правила для Sass; stylelint-config-clean-order - автоматически расставляет CSS-свойства в логическом порядке (сначала позиционирование, потом размеры, потом шрифты и т.д.).
Создаём в apps/web файл .stylelint.config.mjs и заполняем его. Затем в package.json фронтенда добавляем скрипт "lint:style": "stylelint \"src/**/*.scss\"" для запуска проверки и "lint:style:fix": "stylelint \"src/**/*.scss\" --fix" для починки.


<!--Установка пакетов при работе с турборепо-->

-Файлы package.json у меня должны быть: в корне проекта, в apps/server/, в /apps/web, в packages/"\*"/
-Устанавливаю пакеты только из корня. Если это общий пакет, то использую стандартную команду "npm install package_name". Если же мне нужно установить пакет для фронтенда, то я выполняю команду "npm install -D package_name -w web".
-Установить пакет:
npm install -D turbo
-Заполнить файл turbo.json (базовая настройка).
-Установим Prettier - для единообразия форматирования кода его ставим для всего проекта. 
Выполним команду:
npm install -D prettier prettier-plugin-packagejson
Пакеты: prettier - это сам движок.
prettier-plugin-packagejson - это плагин, который будет автоматически сортировать поля в многочисленных package.json (в корне, в apps, в packages) по алфавиту и стандарту.
В корне проекта создадим файл ".prettierrc".
Из корня выполним команду:
npm i -D stylelint-config-prettier-scss -w web
, чтобы prettier и stylelint не конфликтовали.
После этого в stylelint.config.mjs на фронтенде в массив extends в самый конец добавим строку:
"stylelint-config-prettier-scss".
Далее занимаемся устранением конфликтов между Prettier и ESLint. Выполним команду:
npm install -D eslint-config-prettier -w web
После этого в esling.config.js фронтенда добавим строку "eslintConfigPrettier" (в самый конец; не забыть импорт).
-Инициализирую Git в корне проекта. Файл gitignore всего один и он в корне.

/////////////////////////////////
//////Создаю типы для валидации
-Выполняю "npm init -y" в packages/validation.
-Правлю package.json (в т.ч. имя ставлю как @repo/validation).
-Из корня проекта выполняю команду:
npm install zod -w @repo/validation
-В папке packages/validation создаю файл index.ts и там описываю схемы для регистрации и т.п.. Эти схемы будут применяться и на фронтенде и на бэкенде.
-Добавляем в apps/web/tsconfig.app.json в секцию paths строку (чтобы VSCode мгновенно видел изменения в схемах без пересборки):
"@repo/validation": ["../../packages/validation/index.ts"]
-В корневом package.json добавляем строку:
"packageManager": "npm@11.9.0"
(указываем свою текущую версию, которою можно узнать через "npm -v")
-В packages/validation создаём tsconfig.json с минимальными настройками, а также из корня выполняем команду:
npm install -D typescript -w @repo/validation
-В packages/validation/package.json добавляем скрипт "type-check": "tsc --noEmit". Затем запускаем этот скрипт (для проверки валидности кода в index.ts и проверки работы монорепозитория) из корня командой:
npx turbo type-check
Если скрипт отработал без ошибки, то всё хорошо.

/////////////////////////////////
-Устанавливаю React Hook Form командой из корня:
npm install react-hook-form @hookform/resolvers -w web
Примечание: @hookform/resolvers - это специальный адаптер, который позволяет react-hook-form понимать правила валидации из твоей Zod-схемы.

/////////////////////////////////
///////////Занимаюсь бэкендом
-В apps/server создаю package.json (через npm init -y).
-Устанавливаю пакеты:
npm install express cors dotenv -w server

а также:
npm install -D typescript @types/node @types/express @types/cors ts-node-dev -w server

и ещё:
npm install -D tsx -w server
(чтобы ts-node мог работать с ESM модулями)

-Привязываю свою валидацию также к серверу (из корня):
npm install @repo/validation -w server

-Запускаю сервер из корня командой:
npm run dev -w server

///////////////////////////////
/////Проверяю работу схемы Zod на сервере.
-Запускаю сервер
-В Postman указываю POST запрос для адреса сервера (http://localhost:3001/api/auth/register). В Headers ставлю Content-Type: application/json. В Body выбираю raw --> JSON.

Запрос №1: Отправляю: {}. Должен получить 400 Bad Request.

Запрос №2: Отправляю: {
  "name": "alexander",
  "email": "invalid-email",
  "password": "PasswordAlexander123!"
}
Должен получить ошибку по email и по password.

Запрос №3: Отправляю: {
  "name": "alex_dev",
  "email": "test@google.com",
  "password": "SafePassword@2026"
}
Должен получить 201 Created.

///////////////////////////////
/////Настраиваю ESLint на сервере.
-В apps/server создаю и наполняю tsconfig.json.
-Из корня ставлю пакеты:
npm install -D eslint @eslint/js globals typescript-eslint eslint-config-prettier -w server

-Создаю конфиг apps/server/eslint.config.js

///////////////////////Запускаем БД в Docker:
-Наполним docker-compose.yml данными.
-Собираем командой "docker-compose up -d".
-Проверяем работу после запуска:

Заходим в Adminer по http://localhost:8080. 
Выбираем систему PostgreSQL, сервер postgres, логин admin, пароль password, БД cybersite_db.

Заходим в Mongoexpess по http://localhost:8081/.
Логин - dev, пароль - dev.

Заходим в Redis Insight по http://localhost:5540.

///////////////////////Занимаемся Prisma (создаём модели и таблицы в БД):
-Создаю package.json в packages/database. Правлю его, в т.ч. изменив имя на "@repo/database".
-Из корня устанавливаю Prisma командой:
npm install prisma -w @repo/database

-Внутри папки packages/database выполняю:
npx prisma init --datasource-provider postgresql
Командой инициализирую призму.
-В созданном .env файле в packages/database изменяю DATABASE_URL на нужный (который в Docker).
-Настраиваем prisma.config.ts и generator + datasource в schema.prisma.
-Записываем свои схемы в schema.prisma.

-Выполняю установку клиента Prisma (в packages/database):
npm install @prisma/client -w @repo/database

-Из папки packages/database выполняю (не забыть выключить postgres-службу на своём ПК, чтобы порты не конфликтовали):
npx prisma migrate dev --name init_full_schema
(если предложат сделать reset, то выполняю "npx prisma migrate reset", а затем снова повторяю команду).

-Из папки packages/database выполняю:
npx prisma generate

-Чтобы сервер мог импортировать базу, создаю файл packages/database/index.ts
Экспорты из этого файла используются для связывания сервера и БД.
Для этого файла надо установить пакеты:
npm install @prisma/adapter-pg pg -w @repo/database
npm install -D @types/pg -w @repo/database
////////////////////Делаем регистрацию:
-Из корня выполняем:
npm install argon2 -w server

-Добавляем на сервер логику (эндпоинт, контроллеры) для регистрации.

-Установим пакеты для frontend:
npm install react-hook-form @hookform/resolvers axios -w web
Пакеты: react-hook-form - для работы с формами; @hookform/resolvers - заставит форму понимать Zod-схемы из @repo/validation; 


-Ставим иконки:
npm install react-icons -w web



///////////////////////////
Как выполнять миграцию БД при обновлении схемы:
-Захожу в папку packages/database и выполняю команду:
npx prisma migrate dev --name имя_для_миграции
(при этом БД должна быть запущена; сервер можно не выключать)
-Выполнить npx prisma generate